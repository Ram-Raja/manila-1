---
features:
  - Driver may return ``access_key``, an access credential, for client
    identities granted share access.
  - Added ``access_key`` to the ShareAccessMapping model.
  - Exposed ``access_key`` to the client through the ``access_list`` API.
